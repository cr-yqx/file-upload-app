# 使用指南

## 应用已启动 ✅

Flask 应用已成功启动并运行在：
- **本地访问**: http://127.0.0.1:5000
- **局域网访问**: http://192.168.3.49:5000

## 测试步骤

### 1. 打开浏览器
在浏览器中访问：`http://localhost:5000`

### 2. 测试文件上传

#### 测试场景 A：上传合法图片
1. 点击"选择文件"按钮
2. 选择项目目录中的 `test_image.jpg`（已自动创建）
3. 查看文件信息显示
4. 点击"上传文件"按钮
5. 观察进度条从 0% 到 100%
6. 查看上传成功消息和文件信息
7. 点击访问地址链接，验证文件可访问

#### 测试场景 B：上传 PDF 文件
1. 选择任意 PDF 文件（< 10MB）
2. 验证上传流程

#### 测试场景 C：文件大小限制
1. 尝试选择超过 10MB 的文件
2. 应该看到错误提示："文件大小超过 10MB 限制"

#### 测试场景 D：文件类型限制
1. 尝试选择不支持的文件类型（如 .txt, .doc）
2. 应该看到错误提示："不支持的文件类型，仅支持图片和 PDF"

## 功能验证清单

- [ ] 页面正常加载，样式显示正确
- [ ] 文件选择按钮可用
- [ ] 选择文件后显示文件信息
- [ ] 文件大小验证正常工作
- [ ] 文件类型验证正常工作
- [ ] 上传按钮在选择合法文件后启用
- [ ] 上传过程中显示进度条
- [ ] 进度条百分比实时更新
- [ ] 上传成功后显示成功消息
- [ ] 上传成功后显示文件信息
- [ ] 可以通过链接访问上传的文件
- [ ] 上传失败时显示错误消息

## 上传的文件位置

所有上传的文件保存在：`/d/01python/skill_test/uploads/`

## 停止应用

在命令行中按 `Ctrl+C` 停止 Flask 应用。

## 常见问题

**Q: 页面无法访问？**
A: 确认 Flask 应用正在运行，检查端口 5000 是否被占用。

**Q: 上传失败？**
A: 检查文件大小是否超过 10MB，文件类型是否支持。

**Q: 找不到上传的文件？**
A: 检查 `uploads/` 目录，确认文件已保存。

## 下一步扩展

如果需要增强功能，可以考虑：
- 支持多文件上传
- 添加拖拽上传功能
- 图片预览功能
- 文件列表管理
- 文件删除功能
- 云存储集成（如 AWS S3, 阿里云 OSS）
